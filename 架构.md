# 架构设计说明书

## 1. 项目概述

本项目为一款基于 TI TMS320F28335 DSP 的工业级高精度采样与控制板卡。
核心业务逻辑为：通过 W5500 运行 ModbusTCP 协议与上位机交互，接收参数并配置数字电位计及硬件增益；上电后以 **100 kSPS（100kHz）** 的设计基准持续对 AD7982 进行固定节拍采样，在接收到 CAN 总线触发指令后开始计算区间平均值；CAN 仅作为“开始统计”的发令枪，不负责对齐各板卡的 ADC 采样相位，允许存在少量采样点级别的同步偏差。采样过程不保存整段波形，仅维护实时值、累加值和计数值，最终将实时采样值和区间平均值通过 Modbus 寄存器供上位机读取。

## 2. 硬件外设资源分配策略

为了实现任务解耦并降低实时采样对 CPU 的占用，DSP 外设资源进行如下划分：

| 硬件模块 | 连接外设/芯片 | 核心功能与配置 |
| :--- | :--- | :--- |
| **原生 SPI** (SPI-A) | W5500 | 专用于 ModbusTCP 通讯。主循环轮询处理，不占用实时资源。 |
| **McBSP-B** (SPI主模式) | TPL0501, AD5290（数字电位器） | **普通 GPIO 充当片选 (CS)**。用于低频、非实时控制。支持总线复用机制动态切换时序。 |
| **McBSP-A** (SPI主模式) | AD7982 (18位 ADC) | 结合 **DMA** 使用，McBSP 按 32-bit 字长接收。AD7982 的 CNV 同时按“CS式读数窗口”使用：空闲时保持高电平，读取时拉低，读取结束后拉高并以该上升沿启动下一次转换。 |
| **ePWM** | AD7982 采样链路 | 提供固定的 100kHz 周期触发节拍，用于发起一次 AD7982 读取流程；不要求 CAN 到来时重置或同步 ePWM 相位。 |
| **DMA 通道** | McBSP-A 接收端 | **直接触发源为 McBSP-A 接收事件 MREVTA/REVT，而不是 ePWM。** 一个 ADC 样点由 `DRR2 + DRR1` 两个 16-bit word 组成；`BURST_SIZE=1` 表示每个 burst 搬运 2 个 16-bit word。 |
| **CAN 控制器** | 外部同步设备 | 仅用作“发令枪”，接收触发报文后开启平均值统计窗口；不参与 ADC 采样相位同步。 |
| **CPU Timer0 (50us)** | 系统软件计时 | DSP 工程基础架构保留。当前无必须业务时可仅作为系统 tick/软件计时使用，也可不启用具体任务；不得承载影响 AD7982 采样链路的耗时逻辑。 |
| **I2C** | AT24C512C (EEPROM) | 存储校准数据、系统掉电保存参数，主循环调用。 |
| **普通 GPIO** | 硬件开关 | 用于采样档位切换、增益切换、DA补偿开关及电平转换使能。 |

---

## 3. 软件运行架构 (Data Flow & State Machine)

软件系统彻底抛弃“定时器轮询采样”的思路，采用 **“事件驱动 + 硬件数据搬运”** 的架构设计。

### 3.1 核心全局变量设计（低 RAM 占用方案）

AD7982 输出为 **18位二补码有符号数据**，因此实时值、累加值和最终平均值必须使用有符号类型。DMA 接收缓存仍使用 `Uint32` 保存原始位流。

当前正在进行的统计使用 `Sum_ADC / Count_ADC`；一次统计结束时，DMA ISR 必须先将完成结果锁存到独立的 `Done_Sum / Done_Count`，再允许下一次 CAN 重新清零活动累加器。这样主循环计算上一轮结果时不会被新一轮统计破坏。

```c
volatile Int32  Live_ADC_Value = 0;  // 供 Modbus 随时读取的【当前实时有符号值】
volatile Uint32 Raw_ADC_Value = 0;   // DMA 硬件自动覆盖的【32位原始接收数据】

volatile Int64  Sum_ADC = 0;         // 当前统计窗口的有符号累加器
volatile Uint32 Count_ADC = 0;       // 当前统计窗口已采样点数
volatile Uint32 Target_Count = 0;    // 目标采样点数 (由上位机下发时间推算)
volatile Uint16 Flag_Start_Avg = 0;  // 当前统计窗口开启标志 (CAN触发)

volatile Int64  Done_Sum = 0;        // 最近一次已完成统计的累加和快照
volatile Uint32 Done_Count = 0;      // 最近一次已完成统计的实际点数快照
volatile Uint16 Flag_Calc_Done = 0;  // 已有一组完成结果等待主循环处理
volatile Int32  Final_Average = 0;   // 最终求出的【有符号平均值结果】
```

### 3.2 任务执行引擎 (三层解耦)

系统分为**主循环（后台非实时任务）**、**中断层（前台实时任务）**和**硬件数据链路层**。

#### A. 硬件数据链路层 (Hardware Data Path)

AD7982 采用简单的 **CNV/CS 复用式读取方案**，不使用 Busy 信号，不额外追求 CAN 与 ADC 采样相位的严格同步。

* **基本时序**：
    1. 非读取期间，`CNV` 保持高电平，AD7982 有充分时间完成上一轮转换。
    2. 100kHz 周期触发到来后，将 `CNV` 拉低，开始本轮读取窗口。
    3. McBSP-A 产生串行时钟，读出**上一轮 CNV 上升沿启动转换所得的数据**。
    4. McBSP/DMA 完成本次读取后，将 `CNV` 拉高。
    5. `CNV` 的此次上升沿启动 AD7982 的下一轮转换；转换结果在下一次周期触发时读取。
* **链路**：`100kHz 周期触发 -> CNV 拉低 -> McBSP 读取上一转换结果 -> McBSP MREVTA -> DMA 搬运 DRR2/DRR1 -> Raw_ADC_Value -> CNV 拉高 -> 启动下一次 ADC 转换`。
* **初始化要求**：系统启动时需要先产生一次有效的 `CNV` 上升沿以启动首轮转换，并保证首轮读取前已经留出足够的转换时间。
* **设计取舍**：本项目关注固定节拍下的连续采样和触发后的区间统计，不要求 ADC 的绝对采样时刻与 CAN 报文严格对齐，因此不引入 Busy 检测或额外的 CNV 延时状态机。

* **100 kSPS 时序预算**：
    - 采样周期固定为 **10us**。
    - 若工程配置为 `SYSCLK = 150MHz`、`LSPCLK = 37.5MHz`，首选 `CLKGDV = 3`，此时 McBSP 串行时钟约为 **9.375MHz**。
    - 32-bit 接收耗时约为 `32 / 9.375MHz = 3.41us`，单个 10us 采样周期内剩余约 **6.59us**，可用于转换等待、DMA 搬运、中断响应和必要的软件开销。
    - McBSP 采样率发生器按 `CLKG = LSPCLK / (CLKGDV + 1)` 计算。若实际工程的 `LSPCLK` 不是 37.5MHz，必须根据该公式重新选择 `CLKGDV`。
    - SCK 目标建议落在 **9~12.5MHz**，无需精确等于 10MHz；应结合实际 `LSPCLK`、板级信号完整性和 AD7982 时序实测确认最终值。

#### B. 中断实时处理层 (ISRs)

1. **CAN 接收中断 (开启/重启统计窗口)**
    * **触发条件**：收到特定的触发报文。
    * **业务逻辑**：无论当前是否正在统计，都执行：`Sum_ADC = 0`、`Count_ADC = 0`、`Flag_Start_Avg = 1`。
    * **重复 CAN 规则**：如果上一轮统计尚未完成又收到新的 CAN，**立即放弃当前未完成统计，并从新的 CAN 触发时刻重新开始统计**；不排队、不等待上一轮完成。
    * **完成结果保护**：CAN ISR 只清零活动变量 `Sum_ADC / Count_ADC`，**不得清零或修改 `Done_Sum / Done_Count / Flag_Calc_Done`**。因此即使上一轮已经完成、主循环还没来得及计算平均值，新 CAN 也不会破坏上一轮已锁存结果。
    * **同步定义**：CAN 只决定“从收到触发后的后续采样点开始统计”，**不重置 ePWM、不重启 ADC、不对齐各板卡采样相位**。不同板卡之间允许存在少量采样点级别的起始偏差。
    * **耗时控制**：只做少量赋值，立即退出。

2. **DMA 接收中断 (数据汇聚与测算)**
    * **触发条件**：每个 McBSP-A 接收事件触发 DMA 搬运一个完整 ADC 样点；一个样点包含 `DRR2 + DRR1` 两个 16-bit word。若按“每个样点一次 DMA ISR”的架构配置，则一次 DMA transfer 仅包含一个 burst，并在 transfer 结束时产生 DMA 中断。
    * **业务逻辑**：
        1. 从 `Raw_ADC_Value` 中移位、掩码得到 18位原始码：`raw18 = (Raw_ADC_Value >> 14) & 0x3FFFF`。
        2. 根据 bit17 (`0x20000`) 做 **18位二补码符号扩展**，转换为 `Int32`：

```c
Uint32 raw18 = (Raw_ADC_Value >> 14) & 0x3FFFFUL;
Int32 adc_value = (Int32)raw18;

if (raw18 & 0x20000UL)
{
    adc_value -= 0x40000L;
}
```

        3. 更新实时单点变量 `Live_ADC_Value = adc_value`。
        4. 判断 `Flag_Start_Avg == 1`：若成立，则执行 `Sum_ADC += (Int64)adc_value`，并自增 `Count_ADC`。
        5. 当 `Count_ADC >= Target_Count` 时，必须按以下顺序结束本轮统计：

```c
Done_Sum   = Sum_ADC;      // 先锁存完成结果
Done_Count = Count_ADC;    // 锁存实际完成点数
Flag_Start_Avg = 0;        // 再关闭当前统计窗口
Flag_Calc_Done = 1;        // 最后发布“结果已完成”标志
```

    * **锁存顺序原则**：必须先写 `Done_Sum / Done_Count`，最后再置位 `Flag_Calc_Done`。主循环只允许在看到 `Flag_Calc_Done == 1` 后读取完成快照。
    * **注意**：严禁直接将 `0x20000 ~ 0x3FFFF` 当作正整数累加，否则 AD7982 的负数采样值会被错误解释为大正数。
    * **100 kSPS CPU 预算**：每秒产生 **100,000 次 DMA ISR**。按 `SYSCLK = 150MHz` 计算，每个 10us 采样周期约有 **1500 CPU cycles**。
    * **ISR 允许范围**：DMA ISR 只允许完成 18位符号扩展、`Live_ADC_Value` 更新、`Int64` 累加、计数以及完成结果锁存；不得执行除法、Modbus 处理、延时或任何阻塞操作。
    * **耗时验证**：必须通过 GPIO 翻转、CPU Timer 或其他可重复测量手段实测 DMA ISR 执行时间。实测值应显著低于 10us，最好控制在 **1~2us** 量级，并为 Timer0、CAN 和其他不可避免的中断响应留出裕量。
    * **当前 DMA 粒度**：100 kSPS 模式继续保持**每个样点一次 DMA ISR**，不引入块 DMA 或 ping-pong 缓冲。

#### C. 主循环业务层 (While 1)

负责处理所有耗时、非实时且复杂的逻辑：

1. **ModbusTCP 轮询**：解析上位机读写寄存器请求。
2. **完成结果计算**：检测到 `Flag_Calc_Done == 1` 时，从 `Done_Sum / Done_Count` 读取已锁存快照，而不是读取仍可能被新 CAN 清零的 `Sum_ADC / Count_ADC`。
3. **平均值除法**：执行有符号除法 `Final_Average = (Int32)(done_sum_local / (Int64)done_count_local)`。除法放在主循环，不放在 DMA ISR。
4. **数字电位器动态配置 (总线复用)**：当上位机下发 TPL0501 / AD5290 参数时执行。

为避免主循环读取 64-bit `Done_Sum` 时恰好被 DMA ISR 更新，推荐主循环仅在复制完成快照的几个赋值期间进入一个**极短临界区**，随后立刻恢复中断；耗时的 64-bit 除法必须在临界区外执行：

```c
if (Flag_Calc_Done)
{
    DINT;
    Int64  done_sum_local   = Done_Sum;
    Uint32 done_count_local = Done_Count;
    Flag_Calc_Done = 0;
    EINT;

    if (done_count_local != 0)
    {
        Final_Average = (Int32)(done_sum_local / (Int64)done_count_local);
    }
}
```

### 3.3 平均值统计状态机规则

平均值统计统一遵循以下状态规则，后续代码实现不得自行改变语义：

1. **空闲 + CAN**：清零 `Sum_ADC / Count_ADC`，进入统计状态。
2. **统计中 + 新 CAN**：当前未完成统计立即作废，重新清零 `Sum_ADC / Count_ADC`，从新触发重新统计。
3. **统计达到 Target_Count**：DMA ISR 将结果复制到 `Done_Sum / Done_Count`，关闭当前统计窗口，并置 `Flag_Calc_Done`。
4. **已完成但主循环尚未计算 + 新 CAN**：允许直接开始下一轮统计；新一轮只使用 `Sum_ADC / Count_ADC`，不得覆盖上一轮 `Done_Sum / Done_Count`。
5. **主循环处理完成结果**：复制 `Done_Sum / Done_Count` 到局部变量后清 `Flag_Calc_Done`，再在临界区外完成除法和 Modbus 结果更新。

---

## 4. 关键模块底层设计规范

### 4.1 18位 AD7982 的 McBSP + DMA 接收机制

* **接口模式原则**：采用最简单的无 Busy 读取方式。`CNV` 在非读取期间保持高电平；周期触发到来时拉低 `CNV` 并通过 McBSP 读出上一轮转换结果；读取完成后拉高 `CNV`，利用该上升沿启动下一轮转换。
* **流水关系**：第 N 次读取取得的是上一轮已经完成的转换结果；本次读取结束后的 `CNV` 上升沿启动下一轮转换。因此系统存在固定的一拍流水延迟，但不影响本项目的区间平均统计需求。

#### 4.1.1 DMA 触发与计数单位规范

为了避免把“ADC 样点数、DMA word 数、DMA SIZE 寄存器值”混为一谈，统一采用以下定义：

1. **DMA 的直接外设触发源是 McBSP-A Receive Event：`MREVTA`**。在 F28335 DMA `PERINTSEL` 中对应值为 `15`。ePWM 只负责提供 AD7982 周期读取节拍，不直接作为本接收 DMA 通道的触发源。
2. **McBSP 32-bit 接收字长不等于 DMA 一次搬一个 32-bit 外设寄存器。** McBSP 的接收结果仍由两个 16-bit 数据寄存器组成：
   - `DRR2`：高 16 位
   - `DRR1`：低 16 位
3. **一个 ADC 样点 = 两个 16-bit DMA word = 一个 DMA burst。**
4. F28335 DMA 的各类 `SIZE` 寄存器采用 **N-1 编码**：
   - `BURST_SIZE = 0` 表示 1 个 16-bit word
   - `BURST_SIZE = 1` 表示 2 个 16-bit word
   - 因此本项目接收一个 32-bit McBSP 样点时使用 **`BURST_SIZE = 1`**。
5. 如果保持“每个 ADC 样点产生一次 DMA ISR”的架构，则一个 transfer 只包含一个 burst：**`TRANSFER_SIZE = 0`**；DMA 通道使用连续模式，在本次 transfer 完成后自动重新初始化并等待下一次 `MREVTA`。
6. DMA 中断必须配置为 **transfer 结束时产生**，确保 ISR 运行时 `Raw_ADC_Value` 已经完整更新；不要在 transfer 开始时进入 ISR。
7. `ONESHOT` 保持为“每个外设事件只执行一个 burst”的模式，避免一次 `MREVTA` 后 DMA 无等待地连续执行多个 burst。
8. 当前 100 kSPS 实现明确保留“每样点一次 DMA ISR”的简单架构，不将多个样点合并为块 DMA。

可按下面的语义理解，寄存器字段名以实际 F28335 工程头文件为准：

```c
// 语义示意，不作为可直接复制的完整初始化代码
PERINTSEL    = 15;   // MREVTA: McBSP-A receive event
BURST_SIZE   = 1;    // 2 x 16-bit word = DRR2 + DRR1
TRANSFER_SIZE= 0;    // 1 burst / transfer = 1 ADC sample
CONTINUOUS   = 1;    // 完成后自动等待下一次 MREVTA
ONESHOT      = 0;    // 每个 MREVTA 只执行一个 burst
CHINTMODE    = 1;    // transfer end interrupt
CHINTE       = 1;
PERINTE      = 1;
```

#### 4.1.2 DRR2 / DRR1 到 Raw_ADC_Value 的存放规则

* DMA 必须先读 `DRR2`，再读 `DRR1`。
* C28x 为 16-bit word 地址体系，`Uint32 Raw_ADC_Value` 占用连续两个 16-bit 地址；低地址保存低 16 位，高地址保存高 16 位。
* 因此若希望 DMA 搬运完成后 `Raw_ADC_Value` 自然形成：

```text
Raw_ADC_Value[31:16] = DRR2
Raw_ADC_Value[15:0]  = DRR1
```

则目的地址应从 `Raw_ADC_Value` 的**高 16 位地址**开始，第一拍写入 DRR2，第二拍地址减 1 写入 DRR1。不要简单把目的地址从低 16 位开始再 `+1`，否则高低 16 位可能颠倒。
* 具体 `SRC_BURST_STEP / DST_BURST_STEP / TRANSFER_STEP` 必须结合 C28x 地址单位配置；底层调试时使用固定测试码或已知 ADC 输入确认 32-bit 拼接顺序。

#### 4.1.3 18位有效数据与有符号规则

* **18位数据提取**：若实际 McBSP 接收验证确认有效 18位位于 32位数据的 `[31:14]`，则使用 `(Raw_ADC_Value >> 14) & 0x3FFFF` 提取；最终位对齐需在底层调试时结合已知输入和示波器/逻辑分析结果确认。
* **有符号数据规则**：AD7982 的 18位输出按二补码解释。bit17=0 时范围为 `0 ~ +131071`；bit17=1 时范围为 `-131072 ~ -1`。所有统计计算必须先完成 18位到 `Int32` 的符号扩展，再写入 `Live_ADC_Value` 和 `Int64 Sum_ADC`。

### 4.2 McBSP 控制双数字电位器的总线复用规范

TPL0501 和 AD5290 均按**数字电位器**定位，不再在架构文档中称为 DAC。

在主循环中控制 TPL0501 和 AD5290 两个不同型号数字电位器时，**严禁直接修改配置寄存器后立即发送**，必须遵循以下标准时序：

1. **复位进入**：清零 `SPCR1.RRST`、`SPCR2.XRST`、`SPCR2.GRST`。
2. **修改参数**：根据目标数字电位器写入对应的极性、相位、波特率。
3. **复位释放**：先置位 `GRST`，等待 2 个时钟；再置位 `XRST`、`RRST`。
4. **强制延时**：执行 `DELAY_US(1~2)`，等待硬件状态机完全就绪。
5. **启动传输**：拉低对应 GPIO CS 管脚 -> 写入 `DXR1` 数据。
6. **安全等待**：**禁止使用固定延时猜测发送完成，必须等待 McBSP 对应完成/就绪状态**，随后拉高 GPIO CS。具体使用发送还是接收就绪标志，以最终 McBSP SPI 模式实现验证为准。

### 4.3 中断优先级与嵌套法则

* **优先级排序**：硬件默认分组 Timer0(Group 1) > DMA(Group 7) > CAN(Group 9)。
* **嵌套规则**：**全系统严格禁止中断嵌套！**
* **Timer0 定位**：50us Timer0 来自 DSP 工程基础架构。当前系统如果没有明确的 50us 周期业务，可以只做系统 tick/软件计时，或者保持空闲；它不是 AD7982 采样链路的组成部分，也不负责驱动平均值统计。
* **设计原则**：依靠 F28335 的硬件 PIE 自动排队机制处理并发中断。Timer0、DMA、CAN ISR 都必须保持极短，不得包含延时、阻塞等待、浮点除法或 Modbus 解析。Timer0 后续若增加业务，也不得长时间占用 Group 1 高优先级中断而影响 DMA/CAN 响应。

### 4.4 采样率扩展边界

当前实现范围以 **100 kSPS** 为基准，并保留每样点一次 DMA ISR 的简单架构。若未来采样率提升到约 **500 kSPS**，采样周期将缩短到约 2us，每点中断的固定开销会显著挤压 CPU 预算，因此届时不再推荐沿用当前架构；应改为 DMA 小块或 ping-pong 缓冲，由 CPU 按块完成符号扩展、累加和结果处理。该扩展方案仅作为边界说明，**不属于当前实现范围**。

## 5. 架构优势总结

1. **低 RAM 占用**：不缓存整段采样波形，仅维护当前值、活动累加值和完成快照，长时间运行时 RAM 占用基本固定。
2. **统计窗口简单稳定**：将测量时间转化为固定采样点数（如 100kHz 下 200ms 对应 20000 点）。CAN 仅负责开启或重启统计窗口，不额外承担采样相位同步，符合本项目对同步精度的实际要求。
3. **结果与下一轮采样解耦**：DMA 完成时把 `Sum_ADC / Count_ADC` 锁存到 `Done_Sum / Done_Count`，因此下一次 CAN 可以立即重启统计，而不会破坏上一轮待处理结果。
4. **DMA 语义明确**：一个 ADC 样点固定定义为两个 16-bit DMA word；McBSP 接收事件直接触发 DMA，避免 Agent 将 ePWM、ADC 样点数和 DMA burst/transfer 概念混淆。
5. **响应极快**：上位机既可以随时获取最新实时单点采样，也能在触发后获取指定采样点数对应的区间均值。
6. **扩展性强**：ModbusTCP 与底层采集链路解耦，后续增加更多 Modbus 寄存器或算法，无需改变 AD7982 的基本采样/读取流程。
