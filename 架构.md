# 架构设计说明书

## 1. 项目概述

本项目为一款基于 TI TMS320F28335 DSP 的工业级高精度采样与控制板卡。
核心业务逻辑为：通过 W5500 运行 ModbusTCP 协议与上位机交互，接收参数并配置数字电位计及硬件增益；上电后以 **100 kSPS（100kHz）** 的设计基准持续对 AD7982 进行固定节拍采样，在接收到 CAN 总线触发指令后开始计算区间平均值；CAN 仅作为“开始统计”的发令枪，不负责对齐各板卡的 ADC 采样相位，允许存在少量采样点级别的同步偏差。采样过程不保存整段波形，仅维护实时值、累加值和计数值，最终将实时采样值和区间平均值通过 Modbus 寄存器供上位机读取。

## 2. 硬件外设资源分配策略

为了实现任务解耦并降低实时采样对 CPU 的占用，DSP 外设资源进行如下划分：

| 硬件模块 | 连接外设/芯片 | 核心功能与配置 |
| :--- | :--- | :--- |
| **原生 SPI** (SPI-A) | W5500 | 专用于 ModbusTCP 通讯。主循环轮询处理，不占用实时资源。 |
| **McBSP-B** (SPI主模式) | TPL0501, AD5290（数字电位器） | **固定 SPI Mode 0（CPOL=0、CPHA=0），SCLK 暂定 1MHz，MSB first**；两颗器件共用时钟与数据线，各自使用独立普通 GPIO 作为片选（CS）。用于低频、非实时控制，当前设计不在两颗器件之间动态切换 SPI 极性、相位或波特率。 |
| **McBSP-A** (SPI主模式) | AD7982 (18位 ADC) | 结合 **DMA** 使用，McBSP 按 32-bit 字长接收。AD7982 的 CNV 同时按“CS式读数窗口”使用：空闲时保持高电平，读取时拉低，读取结束后拉高并以该上升沿启动下一次转换。 |
| **ePWM** | AD7982 采样链路 | 提供固定的 100kHz 周期触发节拍，用于发起一次 AD7982 读取流程；不要求 CAN 到来时重置或同步 ePWM 相位。 |
| **DMA 通道** | McBSP-A 接收端 | **直接触发源为 McBSP-A 接收事件 MREVTA/REVT，而不是 ePWM。** 一个 ADC 样点由 `DRR2 + DRR1` 两个 16-bit word 组成；`BURST_SIZE=1` 表示每个 burst 搬运 2 个 16-bit word。 |
| **CAN 控制器** | 外部同步设备 | 仅用作“发令枪”，接收触发报文后开启平均值统计窗口；不参与 ADC 采样相位同步。 |
| **CPU Timer0 (50us)** | 系统软件计时 | DSP 工程基础架构保留。当前无必须业务时可仅作为系统 tick/软件计时使用，也可不启用具体任务；不得承载影响 AD7982 采样链路的耗时逻辑。 |
| **I2C** | AT24C512C (EEPROM) | 存储校准数据、系统掉电保存参数，主循环调用。 |
| **普通 GPIO** | 硬件开关 | 用于采样档位切换、增益切换、DA补偿开关及电平转换使能。 |

---

## 3. 软件运行架构 (Data Flow & State Machine)

软件系统彻底抛弃“定时器轮询采样”的思路，采用 **“事件驱动 + 硬件数据搬运”** 的架构设计。

### 3.1 核心上下文设计（低 RAM 占用方案）

AD7982 输出为 **18位二补码有符号数据**，因此实时值、累加值和最终平均值必须使用有符号类型。DMA 接收缓存仍使用 `Uint32` 保存原始位流。

采样和统计状态统一放入应用层持有的 `Ad7982_Context_t`。当前统计使用 `sum_adc / count_adc`；一次统计结束时，DMA ISR必须先将结果锁存到 `done_sum / done_count`，再允许下一次CAN重新清零活动累加器。

```c
typedef struct
{
    volatile Uint32 raw_adc_value;
    volatile Uint32 frame_count;
    volatile Uint32 launch_overrun_count;
    volatile Uint16 sample_valid;

    volatile int32  live_adc_value;
    volatile Int64  sum_adc;
    volatile Uint32 count_adc;
    volatile Uint32 target_count;
    volatile Uint16 avg_active;

    volatile Int64  done_sum;
    volatile Uint32 done_count;
    volatile Uint16 calc_done;
    volatile int32  final_average;
} Ad7982_Context_t;
```

当前配置阶段只实现到 `raw_adc_value/frame_count/launch_overrun_count/sample_valid`；其余统计成员在后续阶段加入。具体当前结构参见 [`McBSPA_AD7982配置实施说明.md`](McBSPA_AD7982配置实施说明.md)。

### 3.2 任务执行引擎 (三层解耦)

系统分为**主循环（后台非实时任务）**、**中断层（前台实时任务）**和**硬件数据链路层**。

#### A. 硬件数据链路层 (Hardware Data Path)

AD7982 采用简单的 **CNV/CS 复用式读取方案**，不使用 Busy 信号，不额外追求 CAN 与 ADC 采样相位的严格同步。

* **基本时序**：ePWM以100kHz节拍发起一次McBSP-A 32-bit帧；GPIO23复用为MFSXA/CNV，帧开始时自动拉低，帧结束时自动拉高并启动下一次AD7982转换。第N次帧读取上一轮转换结果，存在固定一拍流水延迟。
* **链路**：`ePWM节拍 -> 写DXR启动McBSP帧 -> MFSXA/CNV拉低 -> 32-bit串行读取 -> MFSXA/CNV拉高 -> MREVTA触发DMA CH2 -> DRR2/DRR1搬入AD7982 Context`。
* **设计取舍**：不使用Busy，不要求CAN与ADC采样相位严格对齐；首个接收帧按无效样点处理。
* **时序预算**：采样周期为10us；在`SYSCLK=150MHz`、`LSPCLK=37.5MHz`时，SCK约9.375MHz，32-bit帧约3.41us，剩余时间用于转换、DMA和中断响应。

GPIO复用、McBSP时钟相位、ePWM、DMA CH2、Context、初始化顺序及验证方法参见 [`McBSPA_AD7982配置实施说明.md`](McBSPA_AD7982配置实施说明.md)。

#### B. 中断实时处理层 (ISRs)

1. **CAN 接收中断 (开启/重启统计窗口)**
    * **触发条件**：收到特定的触发报文。
    * **业务逻辑**：无论当前是否正在统计，都执行：`context->sum_adc = 0`、`context->count_adc = 0`、`context->avg_active = 1U`。
    * **重复 CAN 规则**：如果上一轮统计尚未完成又收到新的 CAN，**立即放弃当前未完成统计，并从新的 CAN 触发时刻重新开始统计**；不排队、不等待上一轮完成。
    * **完成结果保护**：CAN ISR只清零活动成员 `sum_adc/count_adc`，不得修改 `done_sum/done_count/calc_done`。因此即使上一轮已经完成、主循环还没来得及计算平均值，新CAN也不会破坏上一轮结果。
    * **同步定义**：CAN 只决定“从收到触发后的后续采样点开始统计”，**不重置 ePWM、不重启 ADC、不对齐各板卡采样相位**。不同板卡之间允许存在少量采样点级别的起始偏差。
    * **耗时控制**：只做少量赋值，立即退出。

2. **DMA 接收中断 (数据汇聚与测算)**
    * **触发条件**：每个 McBSP-A 接收事件触发 DMA 搬运一个完整 ADC 样点；一个样点包含 `DRR2 + DRR1` 两个 16-bit word。若按“每个样点一次 DMA ISR”的架构配置，则一次 DMA transfer 仅包含一个 burst，并在 transfer 结束时产生 DMA 中断。
    * **业务逻辑**：
        1. 从 `context->raw_adc_value` 中移位、掩码得到18位原始码。
        2. 根据bit17（`0x20000`）做18位二补码符号扩展，转换为 `int32`：

```c
Uint32 raw18 = (context->raw_adc_value >> 14) & 0x3FFFFUL;
int32 adc_value = (int32)raw18;

if ((raw18 & 0x20000UL) != 0U)
{
    adc_value -= (int32)0x40000UL;
}
```

        3. 更新 `context->live_adc_value = adc_value`。
        4. 若 `context->avg_active != 0U`，则累加 `sum_adc`并自增`count_adc`。
        5. 当 `count_adc >= target_count` 时，必须按以下顺序结束本轮统计：

```c
context->done_sum   = context->sum_adc;
context->done_count = context->count_adc;
context->avg_active = 0U;
context->calc_done  = 1U;
```

    * **锁存顺序原则**：必须先写 `done_sum/done_count`，最后再置位 `calc_done`。主循环只允许在看到 `calc_done != 0U` 后读取完成快照。
    * **注意**：严禁直接将 `0x20000 ~ 0x3FFFF` 当作正整数累加，否则 AD7982 的负数采样值会被错误解释为大正数。
    * **100 kSPS CPU 预算**：每秒产生 **100,000 次 DMA ISR**。按 `SYSCLK = 150MHz` 计算，每个 10us 采样周期约有 **1500 CPU cycles**。
    * **ISR允许范围**：DMA ISR只允许完成18位符号扩展、实时值更新、累加、计数和结果锁存；不得执行除法、Modbus处理、延时或阻塞操作。
    * **耗时验证**：必须通过 GPIO 翻转、CPU Timer 或其他可重复测量手段实测 DMA ISR 执行时间。实测值应显著低于 10us，最好控制在 **1~2us** 量级，并为 Timer0、CAN 和其他不可避免的中断响应留出裕量。
    * **当前 DMA 粒度**：100 kSPS 模式继续保持**每个样点一次 DMA ISR**，不引入块 DMA 或 ping-pong 缓冲。

#### C. 主循环业务层 (While 1)

负责处理所有耗时、非实时且复杂的逻辑：

1. **ModbusTCP 轮询**：解析上位机读写寄存器请求。
2. **完成结果计算**：检测到 `context->calc_done != 0U` 时，从 `done_sum/done_count` 读取已锁存快照，而不是读取仍可能被新CAN清零的活动累加器。
3. **平均值除法**：在主循环中完成有符号除法并更新 `context->final_average`，不得放入DMA ISR。
4. **数字电位器配置**：当上位机下发 TPL0501 / AD5290 参数时，通过固定的 McBSP-B SPI 时序发送对应数据，仅切换目标器件的 GPIO CS，不动态重配 SPI 模式或频率。

为避免主循环读取64-bit `done_sum` 时恰好被DMA ISR更新，主循环只在复制快照时进入极短临界区，除法必须在临界区外执行：

```c
if (context->calc_done != 0U)
{
    DINT;
    Int64  done_sum_local   = context->done_sum;
    Uint32 done_count_local = context->done_count;
    context->calc_done = 0U;
    EINT;

    if (done_count_local != 0U)
    {
        context->final_average = (int32)(done_sum_local / (Int64)done_count_local);
    }
}
```

### 3.3 平均值统计状态机规则

平均值统计统一遵循以下状态规则，后续代码实现不得自行改变语义：

1. **空闲 + CAN**：清零活动累加器，进入统计状态。
2. **统计中 + 新CAN**：当前未完成统计立即作废，重新清零活动累加器。
3. **统计达到目标点数**：DMA ISR将结果复制到完成快照，关闭统计窗口并置完成标志。
4. **已完成但主循环尚未计算 + 新CAN**：允许直接开始下一轮统计，但不得覆盖上一轮完成快照。
5. **主循环处理完成结果**：复制完成快照、清完成标志，再在临界区外完成除法和Modbus结果更新。

---

## 4. 关键模块底层设计规范

### 4.1 18位 AD7982 的 McBSP + DMA 接收机制

本模块采用以下固定原则：

- GPIO21/22/23分别用于MDRA、MCLKXA和MFSXA/CNV。
- McBSP-A按单帧32-bit、SCK空闲低、上升沿采样配置；MFSXA自动形成CNV读数窗口。
- ePWM提供100kHz节拍，通过极短ISR写DXR发起一帧，不直接触发接收DMA。
- DMA CH2由`MREVTA`触发，一个样点搬运`DRR2 + DRR1`两个16-bit word；片内ADC DMA CH1继续保留。
- DMA结果写入应用层持有的`Ad7982_Context_t.raw_adc_value`，驱动内部不保存分散全局状态。
- 当前保持每样点一次DMA ISR，不引入块DMA或ping-pong缓冲。
- 经下板确认有效18位位于`[31:14]`后，再按18位二补码扩展为`int32`；所有统计必须在符号扩展后进行。

具体寄存器值、DMA地址步进、初始化顺序、首样点处理、ISR包装和下板检查参见 [`McBSPA_AD7982配置实施说明.md`](McBSPA_AD7982配置实施说明.md)。代码实现同时遵循 [`C语言代码编写规范.md`](C语言代码编写规范.md)。

### 4.2 McBSP 控制双数字电位器的总线复用规范

TPL0501 和 AD5290 均按**数字电位器**定位。两颗器件的串行写入时序统一，因此 McBSP-B 在当前项目中采用**固定配置**，不为不同数字电位器动态切换 SPI 参数。

#### 4.2.1 固定 SPI 参数

- **SPI 模式**：Mode 0，即 `CPOL=0`、`CPHA=0`。
- **SCLK 空闲电平**：低电平。
- **数据采样边沿**：SCLK 上升沿采样。
- **发送顺序**：MSB first，单次写入按 8-bit 数据帧处理。
- **SCLK 频率**：当前统一暂定 **1MHz**。
- **片选方式**：TPL0501 与 AD5290 共用 McBSP-B 的时钟和发送数据线，各使用独立普通 GPIO 作为低有效 CS；任一时刻只能选中一颗器件。

频率选择以**稳定性和时序裕量优先**。TPL0501 支持的串行时钟上限高于 AD5290，而 AD5290 的 SPI 时钟上限为 4MHz；当前业务只在参数变化时偶发写数字电位器，不追求通信吞吐量，因此统一使用 1MHz。相对 AD5290 的 4MHz 上限仍保留至少 4 倍频率裕量；1MHz 下 1 bit 为 1us，8-bit 有效数据仅约 8us，对主循环负担可以忽略。后续若无明确性能需求，不应为了缩短这几微秒而提高数字电位器 SPI 时钟。

#### 4.2.2 初始化与发送原则

1. **一次初始化**：系统初始化阶段配置 McBSP-B 的 Mode 0、1MHz、8-bit、MSB first，并按 McBSP 要求完成 `GRST/XRST/RRST` 等复位释放流程。正常运行后保持该配置，不因目标器件从 TPL0501 切换到 AD5290（或反向切换）而重置、重配 McBSP。
2. **默认取消选中**：初始化完成后两颗数字电位器的 GPIO CS 均保持高电平。
3. **选择目标器件**：发送前确认另一颗器件 CS 为高，再将目标器件 CS 拉低；两颗器件的 CS 严禁同时为低。
4. **保留片选裕量**：CS 拉低后可保留约 `1us` 的保护延时，再启动 McBSP 发送。该延时远大于器件最小时序要求，当前低频业务不需要为此做极限优化。
5. **启动传输**：写入 `DXR1`，由 McBSP-B 输出 8 个 SCLK 脉冲和对应数据。
6. **等待真正帧结束**：**禁止仅靠固定延时猜测发送完成，也不能把“DXR 已空/可写下一字”直接等同于最后一个 SCLK 已结束。** 最终驱动必须采用经 F28335 McBSP SPI 模式验证、能够确认移位发送已经结束的状态条件；如所选状态只能表示发送寄存器已被搬空，则还需结合实际时钟状态或额外保护时间，确保第 8 个采样上升沿和最后一个时钟周期已经完整结束。
7. **释放片选**：确认整帧完成后，可再保留约 `1us` 的保护裕量，然后将目标器件 CS 拉高。数字电位器通信频率很低，优先保证片选与末位时钟之间有充足裕量。

**当前实现原则：McBSP-B 的 SPI 参数是固定资源配置，不再属于“总线复用时动态切换的参数”。所谓双器件总线复用，仅指两颗数字电位器共享 SCLK/SDI，通过两个独立 GPIO CS 选择目标器件。**

### 4.3 中断优先级与嵌套法则

* **优先级排序**：硬件默认分组 Timer0(Group 1) > DMA(Group 7) > CAN(Group 9)。
* **嵌套规则**：**全系统严格禁止中断嵌套！**
* **Timer0 定位**：50us Timer0 来自 DSP 工程基础架构。当前系统如果没有明确的 50us 周期业务，可以只做系统 tick/软件计时，或者保持空闲；它不是 AD7982 采样链路的组成部分，也不负责驱动平均值统计。
* **设计原则**：依靠 F28335 的硬件 PIE 自动排队机制处理并发中断。Timer0、DMA、CAN ISR 都必须保持极短，不得包含延时、阻塞等待、浮点除法或 Modbus 解析。Timer0 后续若增加业务，也不得长时间占用 Group 1 高优先级中断而影响 DMA/CAN 响应。

### 4.4 采样率扩展边界

当前实现范围以 **100 kSPS** 为基准，并保留每样点一次 DMA ISR 的简单架构。若未来采样率提升到约 **500 kSPS**，采样周期将缩短到约 2us，每点中断的固定开销会显著挤压 CPU 预算，因此届时不再推荐沿用当前架构；应改为 DMA 小块或 ping-pong 缓冲，由 CPU 按块完成符号扩展、累加和结果处理。该扩展方案仅作为边界说明，**不属于当前实现范围**。

## 5. 架构优势总结

1. **低 RAM 占用**：不缓存整段采样波形，仅维护当前值、活动累加值和完成快照，长时间运行时 RAM 占用基本固定。
2. **统计窗口简单稳定**：将测量时间转化为固定采样点数（如 100kHz 下 200ms 对应 20000 点）。CAN 仅负责开启或重启统计窗口，不额外承担采样相位同步，符合本项目对同步精度的实际要求。
3. **结果与下一轮采样解耦**：DMA完成时把活动累加器锁存到完成快照，因此下一次CAN可以立即重启统计，而不会破坏上一轮待处理结果。
4. **DMA 语义明确**：一个 ADC 样点固定定义为两个 16-bit DMA word；McBSP 接收事件直接触发 DMA，避免 Agent 将 ePWM、ADC 样点数和 DMA burst/transfer 概念混淆。
5. **响应极快**：上位机既可以随时获取最新实时单点采样，也能在触发后获取指定采样点数对应的区间均值。
6. **扩展性强**：ModbusTCP 与底层采集链路解耦，后续增加更多 Modbus 寄存器或算法，无需改变 AD7982 的基本采样/读取流程。
