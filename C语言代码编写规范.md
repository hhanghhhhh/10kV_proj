# C语言代码编写规范

## 1. 总体原则

- 代码以正确、清晰、简单和可验证为优先目标。
- 只做满足当前需求的设计，禁止为了“分层”引入无实际价值的Manager、Service、Adapter或多层转发。
- 默认结构应是“调用方 -> 模块函数”；涉及硬件中断时最多增加一层薄ISR包装。
- 一个简单函数能完成的逻辑，不拆成多层小函数；只有明确复用、独立职责或便于测试时才拆分。
- 修改旧代码时只整理本次涉及范围，不进行无关重构。

## 2. 命名

- 所有导出函数和类型必须带模块前缀，例如 `IncPI_`、`Ad7982_`。
- 结构体类型使用PascalCase并以 `_t` 结尾，例如 `IncPI_Controller_t`、`Ad7982_Context_t`。
- 函数使用 `Module_Action` 格式，例如 `IncPI_Init`、`IncPI_Run`、`IncPI_Reset`。
- 内部变量、参数和结构体成员使用小写snake_case，例如 `prev_error`、`output_val`。
- 宏使用带模块前缀的全大写名称，例如 `INCPI_MAX_LIMIT`、`AD7982_EPWM_TBPRD`。
- 名称应直接表达用途，避免含义不清的缩写和 `Func1`、`Process2` 等名称。

## 3. 数据类型

工程数值类型统一使用：

```c
Uint32
Uint16
int32
int16
float32
```

- 标志位使用 `Uint16`，取值使用 `0U/1U`。
- 无符号字面量带 `U`，单精度浮点带 `F`。
- 接口和持久状态中不得混用 `int`、`long`、`double` 等平台相关类型。
- 64-bit类型仅在范围确实需要时使用，高频路径使用前必须评估执行时间。

## 4. 状态与Context

- 无运行状态的模块直接使用普通函数，不强制创建Context。
- 有运行状态的模块将状态统一封装在 `Module_Context_t` 中。
- Context由应用层创建并保证生命周期，模块函数通过Context Pointer访问状态。
- 驱动内部禁止定义分散的全局状态变量，也禁止保存静态Context Pointer。
- 允许应用层持有一个根Context，Main、ISR或任务将子模块Context指针传入对应函数。
- ISR不能传参时，使用一层应用ISR包装传递Context；不得继续增加转发层。

```c
typedef struct
{
    Uint32 run_count;
    Uint16 enabled;
} Control_Context_t;

void Control_Run(Control_Context_t *context);
```

## 5. 模块与接口

- 一个模块原则上使用一对 `.h/.c` 文件。
- 只公开调用方需要的类型、宏和函数，其余内容使用 `static`。
- 初始化函数必须显式初始化全部Context成员。
- 输入只读时使用 `const`。
- 禁止隐藏调用顺序、隐式绑定和不明确的跨模块副作用。
- 禁止动态内存和递归；循环必须有明确上界。

## 6. ISR与共享数据

- ISR必须短小、固定执行时间、无阻塞。
- ISR中禁止延时、等待循环、日志、协议解析、复杂浮点和除法。
- ISR只做必要的硬件操作、最小数据处理、计数、置标志和ACK；耗时逻辑放到Main。
- 本工程禁止中断嵌套，ISR内不得执行 `EINT`。
- ISR、Main或DMA共享的数据使用 `volatile`，但 `volatile`不保证原子性。
- 多字数据并发访问使用短临界区、快照或双缓冲；临界区内禁止耗时计算。
- 每个共享成员应明确主要写入方，避免多个执行环境同时修改。

## 7. 执行效率

- 高频函数和ISR必须明确调用频率、周期预算和最坏执行时间。
- 高频路径不做重复初始化、无意义拷贝、动态分配、通用回调或多层函数转发。
- 小型高频函数可使用 `static inline`；普通函数不为追求形式强制内联或拆分。
- 大结构体通过指针传递，禁止按值复制。
- 除法、64-bit、浮点和库函数优先放在Main或低频任务。
- 外设未就绪时记录错误并退出，禁止在高频路径阻塞等待。
- 优化必须以时序预算、实测或反汇编为依据，不做未经验证的技巧性优化。

## 8. 硬件相关代码

- 寄存器配置应说明关键频率、极性、位宽或时序依据。
- 对顺序敏感的寄存器访问必须保持明确顺序，不封装到难以看出时序的多层接口中。
- DMA目标Context必须具有稳定生命周期和正确对齐。
- 写1清除、PIE ACK和受保护寄存器操作必须按芯片手册执行。

## 9. 注释

- 注释说明设计原因、硬件含义、时序关系、计算依据和不可破坏的约束，不复述代码。
- 文件或模块可用一段短注释概述数据链路和占用资源；函数内部只注释关键步骤。
- 关键寄存器组合、DMA地址与步进、初始化顺序和特殊边界处理必须说明原因或结果。
- 显而易见的赋值、判断、循环和计数不写逐行注释；代码变化后同步更新相关注释。
- 注释力求短而准确，复杂细节放入设计文档，代码中只保留理解和维护所需信息。

## 10. 编码检查

提交代码前只需重点确认：

- 命名和数据类型是否符合规范。
- 结构是否足够简单，是否存在无必要的抽象层。
- 有状态模块是否通过Context Pointer工作。
- 驱动内部是否存在隐藏或分散的全局状态。
- ISR是否短小、无阻塞、无 `EINT`。
- 共享多字数据是否处理一致性。
- 高频路径是否满足执行时间预算。
- 是否只修改了当前任务范围。
